<%= form_tag pages_search_path, :class => "search-users", :remote => true do %>
		<%= select(:view, :cohort, @cohort.map(&:name), {:include_blank => "Filter by Cohort"}) %>
    <%= text_field_tag :search, params[:search], :placeholder => "Search by Name" %>
    <%= submit_tag "Go"%>
<% end %>

