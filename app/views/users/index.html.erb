<div id='home_page_header'> 

<%if current_user %>
  <% @existing_users.each do |user| %>
    <% if user.photo_url %>
      <%= link_to (image_tag user.photo_url), "/users/#{user.id}" %>
    <% end %> 
  <% end %>
<% end %>
</div>
