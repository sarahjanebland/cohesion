<div id='home_page_header'> 
<h2>
  The awesome Banana Slugs
</h2>
<%if current_user %>
<% @users.each do |user| %>
  <%= link_to image_tag(avatar(user.photo_url, @avatars)), "/users/#{user.id}" %>  
  <% end %>

<% end %>
</div>
